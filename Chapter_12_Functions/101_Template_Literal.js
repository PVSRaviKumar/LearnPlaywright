//template literal uses  $ followed with flower brackets { } with character  ``


// Type 4 function 
function greetname(name) {
    return `Hello ,  ${name}`;
}

let result = greetname("Madhu");
console.log(result);