// to declare an array
let fruit = []; //empty array declaration
let fruits_fresh = ["Manago", "banna", "apple"];
//size of array is 3
//array index starts with zero

let arra = [10, 34, 56, 78];
// size is 4
// array index is 0-3

//to print the values
console.log(arra[0]);
console.log(arra[1]);
console.log(arra[2]);
console.log(arra[3]);
console.log(arra[4]);// output as undefined in JS in , no error
// java we will get  array index of bound expectation

let testresults = ["pass", "fail", "skipped"];
let mixed = [1, "hello", true, null];//JS arrays it can hold mixture of elements
//JS arraya ca hold any type.

